name: bamboo workflow
  on:
    push:
      branches: [main]
  jobs:
    demo:
      runs-on: ubuntu-latest
      steps:
        - name: greetings
        run: echo "hello bondhu, first workflow done"
